---
const { label, identifier, classes } = Astro.props
---

<div class="flex flex-col gap-2">
	<label for={identifier}>{label}</label>
	<input
		class={`border border-foreground rounded-sm bg-basecolor px-2 py-1 ${classes}`}
		type="text"
		name={identifier}
		id={identifier}
	/>
</div>
